## VectorNav

Everything in this folder is simply the default code provided by VectorNav on their website **except** for `rfr26vectornav.py` which is a python script that logs vector nav data. **This script is still in progress**.
